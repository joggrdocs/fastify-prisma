# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint nyc coverage output
coverage
# don't lint scripts
scripts
# don't lint test setup
jest-setup.js
# don't lint linter
.eslintrc.js
# don't lint yarn generated files
yarn.lock
.pnp.cjs
.pnp.loader.mjs